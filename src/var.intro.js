    
    var
        undefined,
        SIZE = 5 * 1024 * 1024,
        TABLE = " TABLE ",
        DROP = "DROP",
        EXISTS = " EXISTS ",
        IF = "IF",
        autoIncrement = "id INTEGER PRIMARY KEY AUTOINCREMENT",
        Array = window.Array,
        Math = window.Math,
        max = Math.max,
        concat = [].concat,